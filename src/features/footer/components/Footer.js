import React from "react";

const Footer = () => {
    return (
        <footer>
            Service MarketPlace Dapp - © Copyright {new Date().getFullYear()}
        </footer>
    );
};

export default Footer;
